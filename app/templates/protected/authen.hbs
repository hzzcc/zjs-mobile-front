{{partial "components/header"}}

<div class="content-wrap">
  <div class="row ">
    <form {{action 'authencate' this on='submit'}}  method="post" class="mt20" id="resetPassword">
      <div {{bind-attr class=":alert :alert-danger hasError::hidden"}}>
        <i class="fa fa-warning"></i>
        <span>{{this.errorMsg}}</span>
      </div>
      <div class="input-group mb10">
        <span class="input-group-addon text-center">
          <i class="icon-user"></i>
        </span>
        {{input class="form-control placeholder-no-fix" type="text" autocomplete="off" placeholder="请输入您的真实姓名" value=model.real_name}}
      </div>
      <div class="input-group mb10">
        <span class="input-group-addon text-center">
          <i class="icon-idcard2"></i>
        </span>
        {{input class="form-control placeholder-no-fix" type="text" autocomplete="off" placeholder="请输入您的身份证号" value=model.id_card_number}}
      </div>

      <div class="pl30 pr30 text-center mb10">
        <i class="fa fa-exclamation-circle red"></i>请正确填写您的姓名和身份证号，您最多只有两次验证机会。
      </div>

      <div class="row">
        <div class="col-md-12 text-center">
          <button id="submit-btn" class="btn-u btn-u-red col-sm-6 w150" type="submit">提交</button>
        </div>
      </div>

    </form>
  </div>
</div>
